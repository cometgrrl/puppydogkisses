---
layout: layouts/base.njk
eleventyNavigation:
  key: Roxy
  order: 4
---

<h1>Roxy</h1>
{% block content %}
    {% for post in collections.Roxy %}
        <article>
            <img src="{{ post.data.image }}" alt="{{ post.data.imageAlt }}">
            <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
            <p>{{ post.date | readableDate("LLLL d, yyyy") }}</p>
            <p>{{ post.data.description }}</p>
        </article>
    {% endfor %}
{% endblock %}